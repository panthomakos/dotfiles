---
- hosts: localhost
  connection: local
  user: pan
  become: yes
  vars:
    user:
      name: pan
      groups:
        - audio
        - docker
        - users
        - video
        - wireshark
    home: /home/pan
    ruby: 2.2.4
    go: 1.5.1
    docker_compose: 1.5.0
  roles:
    - system
