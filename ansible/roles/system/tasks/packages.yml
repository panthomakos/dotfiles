---
- name: Configure archlinuxfr SigLevel
  ini_file: dest=/etc/pacman.conf
            section=archlinuxfr
            option=SigLevel
            value=Never
- name: Configure archlinuxfr Server
  ini_file: dest=/etc/pacman.conf
            section=archlinuxfr
            option=Server
            value=http://repo.archlinux.fr/{{ ansible_architecture }}
- include: package-bundle.yml pkg=base-devel
- include: package-bundle.yml pkg=lxde
- include: package-bundle.yml pkg=texlive-most # Used for pandoc PDF conversions.
- name: Install Pacman Packages
  pacman: name={{ item }} state=present
  with_items:
    - abs
    - alsa-oss
    - alsa-utils
    - avahi
    - bc
    - bluez
    - bluez-utils
    - chromium              # Browser.
    - coreutils
    - cups                  # Printer management.
    - curl
    - evince
    - expac
    - dnsutils
    - doxygen               # C Development: documentation generation.
    - gdb                   # C Development: debugger.
    - ghostscript
    - glibc                 # C Development: GNU C Library.
    - gnupg
    - gnuplot               # GNU command line plotting utility
    - gsfonts
    - gsl                   # C Development: GNU Scientific Library.
    - gutenprint
    - gvim
    - icedtea-web
    - irssi
    - keepassx
    - libcups
    - libmtp                # MTP filesystem support for Kindle.
    - linux-headers
    - maim                  # Screen capture utility.
    - markdown
    - memcached
    - msmtp
    - mtpfs                 # MTP filesystem for Kindle.
    - net-tools
    - nfs-utils
    - nmap
    - openssh
    - openssl
    - openvpn
    - polkit
    - pulseaudio
    - pulseaudio-alsa
    - pygtk                 # Used by avahi-discover.
    - python-dbus           # Used by avahi-discover.
    - redis
    - rxvt-unicode          # Simple terminal when using X.
    - s3cmd                 # For syncing S3 buckets locally.
    - sane
    - screen
    - slop                  # Allows selection with maim.
    - sxiv                  # Image Viewer
    - tar
    - tcpdump
    - tmux
    - traceroute
    - tree
    - valgrind              # C Development: test for memory usage errors.
    - vlc                   # Video player.
    - w3m
    - xautolock
    - xclip
    - xf86-input-synaptics
    - xf86-video-fbdev
    - xmonad
    - xmonad-contrib
    - xorg-server-xvfb
    - xorg-xbacklight
    - xorg-xdpyinfo         # Get info about X server, like screen resolution.
    - xorg-xinit
    - xorg-xprop            # Display window properties by clicking.
    - xv
    - yaourt                # Provides easy installation for AUR packages.
    - zip                   # ZIP Utility
- include: yaourt.yml pkg=chromium-pepper-flash
